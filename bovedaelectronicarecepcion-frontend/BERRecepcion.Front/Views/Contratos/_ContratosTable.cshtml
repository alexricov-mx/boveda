@using BERRecepcion.Front.Models.Dto
@model DataResult<IEnumerable<SupplyOrderDto>>

@if (Model != null && Model.Data != null && Model.Data.Count() > 0)
{
    <div class="col-lg-12 col-md-12 col-sm-12 text-left">
        <h6>Información del contrato</h6>
        <small>Seleccione el documento a firmar</small>
    </div>
    <div class="card-body">
        <table class="table table-hover" id="contratos-table" style="font-size:14px;">
            <thead class="thead-light">
                <tr>
                    <th>Organismo</th>
                    <th>No. Contrato</th>
                    <th>No. OS</th>
                    <th>Tipo</th>
                    <th>Acreedor</th>
                    <th>RFC</th>
                    <th>Total</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Organismo</td>
                    <td>No. Contrato</td>
                    <td>No. OS</td>
                    <td>Tipo</td>
                    <td>Acreedor</td>
                    <td>RFC</td>
                    <td>Total</td>
                </tr>
                <tr>
                    <td>Organismo</td>
                    <td>No. Contrato</td>
                    <td>No. OS</td>
                    <td>Tipo</td>
                    <td>Acreedor</td>
                    <td>RFC</td>
                    <td>Total</td>
                </tr>

                @*@foreach (var item in Model.Data)
                    {
                        <tr>
                            <td scope="row">@item.UserName</td>
                            <td scope="row">@item.Seccion</td>
                            <td scope="row">@item.Accion</td>
                            <td scope="row">@item.Descripcion</td>
                            <td scope="row">@item.Fecha.ToString("dd/MM/yyyy HH:mm:ss")</td>
                        </tr>
                    }*@
            </tbody>
            @*@if (Model.Pager.EndPage > 1)
                {
                    <tfoot>
                        <tr>
                            <td colspan="5">
                                <nav aria-label="Page navigation example" id="bitacora-nav">
                                    <ul class="pagination justify-content-center">
                                        <li class="page-item @(Model.Pager.CurrentPage == 1 ? "disabled" : "")" style="cursor:pointer;">
                                            <a class="page-link" href="#" tabindex="-1" data-page_num="@(Model.Pager.CurrentPage - 1)">Anterior</a>
                                        </li>
                                        @for (var p = Model.Pager.StartPage; p <= Model.Pager.EndPage; p++)
                                        {
                                            <li class="page-item @(p == Model.Pager.CurrentPage ? "active" : "")" style="cursor:pointer;">
                                                <a class="page-link" data-page_num="@p">@p</a>
                                            </li>
                                        }
                                        <li class="page-item @(Model.Pager.CurrentPage == Model.Pager.TotalPages ? "disabled" : "")" style="cursor:pointer;">
                                            <a class="page-link" data-page_num="@(Model.Pager.CurrentPage + 1)">Siguiente</a>
                                        </li>
                                    </ul>
                                </nav>
                            </td>
                        </tr>
                    </tfoot>
                }*@
        </table>
    </div>


    <script>
        $(function () {
            DataTableInitialize("contratos-table");
        });
    </script>
}
else
{
    <div class="alert alert-info text-center" role="alert">
        No tiene contratos pendientes.
    </div>
}
