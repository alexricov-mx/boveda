@model bool
@if (!Model)
{
<div class="row">
    <div class="col-12 datos-factura m-0">
        <div class="callout callout-info">
            <h5>
                <i class="fas fa-paragraph mr-2"></i> Addenda Pemex
            </h5>
            <hr />
            <textarea class="form-control" id="txtAddenda" rows="15" placeholder="Por favor ingrese el XML de la addenda"></textarea>
        </div>
    </div>
</div>
}
