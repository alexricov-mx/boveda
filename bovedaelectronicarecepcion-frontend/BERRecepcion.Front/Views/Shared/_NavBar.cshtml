<nav class="main-header navbar navbar-expand navbar-dark bg-pantone7421" style="margin-top: -1px;">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="" role="button"><i class="fas fa-bars"></i></a>
        </li>
        @if (User.Identity.IsAuthenticated)
        {
            <li class="nav-item d-none d-sm-inline-block">
                <a class="nav-link">
                    Bienvenido al portal de Bóveda de Documentos Electrónicos de Petróleos Mexicanos - <strong>@DateTime.Now.ToString("dd/MM/yyyy")</strong>
                </a>
            </li>
        }
    </ul>

    <!-- Right navbar links -->
    <ul class="navbar-nav ml-auto mr-2">
        @if (User.Identity.IsAuthenticated)
        {
            <li class="nav-item">
                <img src="~/images/user-image3.jpg" class="img-circle mr-3 elevation-0" data-widget="control-sidebar" data-slide="true" role="button" style="width:36px;" />
            </li>
        }
        @if (!User.Identity.IsAuthenticated)
        {
            <li class="nav-item">
                <a class="nav-link small-box-footer" asp-area="MicrosoftIdentity" asp-controller="Account" asp-action="SignIn">
                    Iniciar Sesión <i class="fas fa-sign-out-alt"></i>
                </a>
            </li>
        }
    </ul>
</nav>
